# 📚 LightSearch 文档系统

> 基于 Markdown 的文档管理和编辑系统

## 🌟 功能特性

### 文档展示站 (`/docs`)
- ✅ Markdown 渲染
- ✅ 代码语法高亮
- ✅ 自动生成目录
- ✅ 全文搜索
- ✅ 图片预览
- ✅ 响应式设计
- ✅ 侧边栏导航

### 文档编辑器 (`/docs/editor`)
- ✅ 实时 Markdown 预览
- ✅ 丰富的工具栏
- ✅ 键盘快捷键
- ✅ 自动保存草稿
- ✅ 导出 Markdown 和 JSON
- ✅ 文档配置管理

## 📁 目录结构

```
docs/
├── index.html              # 文档主页
├── styles.css              # 样式文件
├── script.js               # 脚本文件
├── editor/                 # 编辑器
│   ├── index.html
│   ├── editor-styles.css
│   └── editor-script.js
├── posts/                  # 文档存放目录
│   ├── getting-started/    # 快速开始
│   │   ├── introduction.md
│   │   ├── introduction.json
│   │   ├── installation.md
│   │   └── quick-start.md
│   ├── guide/              # 用户指南
│   │   ├── basic-usage.md
│   │   ├── search-engines.md
│   │   └── themes.md
│   ├── advanced/           # 高级功能
│   │   ├── custom-engines.md
│   │   ├── theme-development.md
│   │   └── api.md
│   └── misc/               # 其他
│       ├── faq.md
│       ├── changelog.md
│       └── contributing.md
└── assets/                 # 资源文件
    └── images/
```

## 🚀 快速开始

### 1. 访问文档站

打开浏览器访问：
```
file:///path/to/LightSearch/docs/index.html
```

或部署到服务器后访问：
```
https://yourdomain.com/docs/
```

### 2. 添加新文档

#### 方法一：使用编辑器（推荐）

1. 访问 `/docs/editor/index.html`
2. 在编辑器中编写 Markdown
3. 填写文档配置（标题、作者、标签等）
4. 点击"下载 Markdown"和"下载配置"
5. 将文件保存到对应的 `posts/[板块名]/` 目录

#### 方法二：手动创建

1. 在 `posts/[板块名]/` 目录下创建 `name.md` 文件
2. 编写 Markdown 内容
3. 创建对应的 `name.json` 配置文件（可选）

**Markdown 示例** (`example.md`):
```markdown
# 文档标题

这是文档内容...

## 二级标题

更多内容...
```

**配置文件示例** (`example.json`):
```json
{
  "title": "文档标题",
  "description": "文档描述",
  "author": "作者名",
  "date": "2026-02-08",
  "tags": ["标签1", "标签2"],
  "category": "getting-started"
}
```

### 3. 更新导航

编辑 `script.js` 中的 `docsConfig` 对象：

```javascript
const docsConfig = {
    sections: [
        {
            title: '快速开始',
            items: [
                { title: '介绍', path: 'getting-started/introduction' },
                { title: '新文档', path: 'getting-started/new-doc' } // 添加这行
            ]
        }
    ]
};
```

## 📝 编辑器使用

### 工具栏功能

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| **B** | 粗体 | Ctrl+B |
| *I* | 斜体 | Ctrl+I |
| ~~S~~ | 删除线 | - |
| H1/H2/H3 | 标题 | - |
| 引用 | 引用块 | - |
| 代码 | 行内代码 | - |
| 代码块 | 代码块 | - |
| 列表 | 无序/有序列表 | - |
| 表格 | 插入表格 | - |
| 链接 | 插入链接 | Ctrl+K |
| 图片 | 插入图片 | - |

### 键盘快捷键

- `Ctrl+B` - 粗体
- `Ctrl+I` - 斜体
- `Ctrl+K` - 插入链接
- `Tab` - 缩进

### 自动保存

编辑器会每 30 秒自动保存草稿到浏览器本地存储，刷新页面后会自动恢复。

## 🎨 自定义样式

### 修改主题颜色

编辑 `styles.css` 中的 CSS 变量：

```css
:root {
    --color-primary: #0071e3;  /* 主色调 */
    --color-bg: #ffffff;       /* 背景色 */
    --color-text: #1d1d1f;     /* 文字颜色 */
    /* ... */
}
```

### 自定义 Markdown 样式

在 `styles.css` 中修改 `.preview-content` 相关样式。

## 🔍 搜索功能

### 使用搜索

1. 点击顶部搜索框或按 `Ctrl+K`
2. 输入关键词
3. 点击搜索结果跳转到对应文档

### 搜索范围

目前搜索功能支持：
- 文档标题
- 所属分类

## 📸 图片管理

### 添加图片

1. 将图片放到 `assets/images/` 目录
2. 在 Markdown 中引用：

```markdown
![图片描述](../assets/images/example.png)
```

### 图片预览

点击文档中的图片可以全屏预览。

## 🌐 部署

### GitHub Pages

1. 推送代码到 GitHub
2. 启用 GitHub Pages
3. 访问 `https://yourusername.github.io/LightSearch/docs/`

### 自定义服务器

直接将 `docs` 目录部署到 Web 服务器即可。

## 🔧 高级配置

### 添加新的文档分类

1. 在 `posts/` 下创建新目录
2. 更新 `script.js` 中的 `docsConfig`
3. 更新编辑器中的分类选项

### 集成搜索引擎

可以集成第三方搜索服务（如 Algolia）来增强搜索功能。

### 添加评论系统

可以集成 Gitalk、Disqus 等评论系统。

## 📊 技术栈

- **Marked.js** - Markdown 解析
- **Highlight.js** - 代码高亮
- **原生 JavaScript** - 无框架依赖
- **CSS3** - 现代样式

## 🤝 贡献

欢迎提交文档改进建议！

1. Fork 仓库
2. 创建文档分支
3. 提交 Pull Request

## 📄 许可证

GPL-3.0（与主项目一致）

## 🙏 致谢

- [Marked.js](https://marked.js.org/)
- [Highlight.js](https://highlightjs.org/)
- [Vilinko Docs](https://docs.vilinko.com/) - 设计灵感

---

**Made with ❤️ by Sails**
