结论速览

  - 这批输出基本把主链路钉住了：JNI导出 -> sub_99A70(接口指针) -> 业务函数，其中加解密主链分别是 sub_B5F5C ->
    sub_AC6C4（加密）和 sub_B61C0 -> sub_ADB14（解密）。
  - sub_AC6C4 / sub_ADB14 已出现明确语义日志：GenEcdhKeyPair、Ecdh failed、HKDF failed、AesGcmEncrypt/Decrypt failed、
    EcdsaVerify failed，可确认是“ECDH+HKDF+AES-GCM(+签名校验)”这一类混合加密流程。
  - 网络侧是 sub_B716C -> sub_BBDA0，sub_B696C 负责请求头拼装（Accept/User-Agent/Content-Type/Host 等），整体是短连接发
    送接收流程。

  按文件分析

  - E:/TEMP_SHARE/Miniworld_pkg_unpack/迷你世界_1.53.1/lib/arm64-v8a/deeper_static_output/deeper_analysis.txt
      - sub_AC6C4 (0xAC6C4)：命中 GenEcdhKeyPair failed、Ecdh failed、HKDF failed、AesGcmEncrypt failed、
        AesGcmEncryptWithCompress failed、Encrypt Success。
      - sub_ADB14 (0xADB14)：命中 EcdsaVerify failed、OpenSslCryptoUtil failed、AesGcmDecrypt failed、Decrypt Success。
      - sub_B3F94 (0xB3F94)：协议解析和算法选择关键点，命中 not found HEADER_ENCRYPT_ALGO、not found
        HEADER_COMPRESS_ALGO、Not supported enc algo、Not supported compress algo、HybridEcdhDecryptBody Decrypt
        failed。
      - sub_B696C (0xB696C)：明显是 SendRequest 组包器，memcpy/strlen 高频，HTTP风格头字段完整。
      - sub_B716C (0xB716C)：主流程，命中 send, length、recv length、__DoParseHead failed，并调用 sub_BBDA0。
      - sub_BBDA0 (0xBBDA0)：__MakeSocketPrepared，命中 GetHostByName failed、connect suc, socket，包含 setsockopt。
      - sub_99A70 (0x99A70)：仅返回 .bss 全局对象地址，JNI导出函数统一经它拿接口。
      - GetIlinkDeviceInterface (0xC2D08)：__cxa_guard_acquire/release + __cxa_atexit，典型单例懒初始化。
  - E:/TEMP_SHARE/Miniworld_pkg_unpack/迷你世界_1.53.1/lib/arm64-v8a/deeper_static_output/target_callgraph.dot
      - 关键边：JNI六个导出函数都指向 sub_99A70。
      - 关键边：sub_B5F5C -> sub_AC6C4，sub_B61C0 -> sub_ADB14，sub_B716C -> sub_BBDA0。
  - E:/TEMP_SHARE/Miniworld_pkg_unpack/迷你世界_1.53.1/lib/arm64-v8a/deeper_static_output/functions_disasm
      - 可直接按函数文件做人工深挖（重点 AC6C4/ADB14/B3F94/B696C/B716C/BBDA0）。

  你当前可以下结论的点

  - 已能确认不是“单纯AES固定密钥”，而是带 ECDH/HKDF 的协商派生路径。
  - 算法选择和压缩选择由包头字段驱动（HEADER_ENCRYPT_ALGO、HEADER_COMPRESS_ALGO）。
  - 请求封包是应用层自定义+HTTP头式拼接，网络发收在短链路任务里闭环。

  仍待人工确认的关键细节

  1. sub_AC6C4 内部调用的 sub_3B6588/sub_3B6CC8/sub_3B94A8/... 与具体 OpenSSL 原语的一一映射。
  2. sub_B3F94 中 enc algo 枚举值与具体算法（例如 GCM 参数）映射表。
  3. sub_B696C/sub_B716C 里密文区边界（头部长度、body 偏移、tag/nonce拼接顺序）。
  4. IV/Nonce 来源（随机、计数器或混合）。

  如果你要，我下一步可以按这 4 点给你出一份“逐地址人工操作清单”（IDA里每步点哪、看哪几个寄存器/栈槽、如何快速命名）。